// Replaced by RootTabView. Kept as a placeholder so any stray references compile.
import SwiftUI

struct ContentView: View {
    var body: some View {
        RootTabView()
    }
}

#Preview {
    ContentView()
}
